// X/Twitter uses the same card as the other social surfaces; keeping one
// definition means the mark and copy can never drift between them.
export { alt, contentType, size, default } from "./opengraph-image"
